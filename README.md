# App Developer Privacy Hub

这是一个专门为独立开发者设计的**隐私政策托管中心**。  
你可以使用这个项目生成一个静态网站，托管在 GitHub Pages 上，用来向 Google Play 或 App Store 展示你的应用隐私政策。

## 🚀 快速开始

### 1. 安装与运行
确保你的电脑上安装了 [Node.js](https://nodejs.org/)。

```bash
# 安装依赖
npm install

# 启动本地预览 (浏览器访问 http://localhost:5173)
npm run dev
```

### 2. 添加/修改隐私政策
你**不需要**修改复杂的 UI 代码，所有的内容都由一个配置文件管理。

打开 `constants.ts` 文件：

1.  **修改开发者信息**：更改 `DEVELOPER_NAME` 和 `DEVELOPER_EMAIL`。
2.  **添加新 App**：在 `APPS` 数组中复制并添加一个新的对象。

```typescript
// 示例：添加一个新 App
{
  id: "new-app-id", // URL 中会使用这个 ID，例如 /#/app/new-app-id/privacy
  name: "我的新 App",
  description: "这是一个很棒的 App...",
  lastUpdated: "2024-05-20",
  policies: {
    [PolicyType.PRIVACY]: [
      {
        title: "1. 数据收集",
        content: "我们不收集任何数据..."
      }
    ]
  }
}
```

保存文件后，网页会自动更新。

---

## 📦 如何部署到 GitHub Pages (推荐)

我已经为你配置好了自动化部署流程 (CI/CD)。请按照以下步骤操作：

### 1. 推送代码到 GitHub
将本项目的所有代码推送到你的 GitHub 仓库（通常是 `main` 分支）。

### 2. 配置 GitHub Pages 来源
这是一次性设置：
1.  进入你的 GitHub 仓库页面。
2.  点击顶部的 **Settings (设置)** 选项卡。
3.  在左侧侧边栏找到 **Pages**。
4.  在 **Build and deployment** 下的 **Source** 下拉菜单中，选择 **GitHub Actions**。
    *   *注意：不需要选择分支，因为 Actions 会自动处理。*

### 3. 触发部署 (手动触发)
虽然代码推送到 `main` 分支会自动触发部署，你也可以随时手动重新部署：
1.  点击仓库顶部的 **Actions** 选项卡。
2.  在左侧列表中点击 **Deploy to GitHub Pages**。
3.  在右侧你会看到一个 **Run workflow** 的下拉按钮。
4.  点击它，确保分支是 `main`，然后点击绿色的 **Run workflow** 按钮。
5.  等待约 1-2 分钟，部署完成后，流程图会变成绿色。

### 4. 获取网址
部署成功后，在 **Settings -> Pages** 页面顶部，你会看到你的网站地址（例如 `https://yourname.github.io/privacy-hub/`）。

---

## 🔗 获取隐私政策链接

对于 Google Play Console，你需要填写的具体链接格式如下：

*   **App 1 隐私政策**: `https://<你的网址>/#/app/award-account/privacy`

**注意**：链接中包含 `/#/` 是因为我们使用了 Hash 路由，这对兼容 GitHub Pages 这种静态托管非常重要，请务必保留。

---

## 🛠 技术细节

*   **框架**: React + TypeScript
*   **构建工具**: Vite
*   **样式**: Tailwind CSS (通过 CDN 引入，保持轻量)
*   **路由**: React Router (HashRouter 模式)
*   **部署**: GitHub Actions (配置文件在 `.github/workflows/deploy.yml`)
